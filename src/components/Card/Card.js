// Card.js
// Licensed under the MIT license in the root directory of this project

import React from 'react';

function Card({ title, data }) {
  <div>
    <h1>{title}</h1>
    <p>{data}</p>
  </div>;
}
export default Card;
